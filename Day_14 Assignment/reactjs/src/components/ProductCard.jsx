import React from "react";

const ProductCard = () => {
  throw new Error("ProductCard crashed!");
  // eslint-disable-next-line no-unreachable
  return <div>Product Card</div>;
};

export default ProductCard;
